package routes

import (
	"github.com/gin-gonic/gin"
)

func initializeScoresRoutes(r *gin.RouterGroup) {
	// scoreRoutes := r.Group("/scores")
	// {
	// 	scoreRoutes.GET("", controllers.GetPlayerScores)          // Récupérer les scores avec pagination
	// 	scoreRoutes.POST("", controllers.CreatePlayerScore)       // Créer un score
	// 	scoreRoutes.GET("/:id", controllers.GetPlayerScore)       // Récupérer un score spécifique
	// 	scoreRoutes.PUT("/:id", controllers.UpdatePlayerScore)    // Mettre à jour un score
	// 	scoreRoutes.DELETE("/:id", controllers.DeletePlayerScore) // Supprimer un score
	// }
}
